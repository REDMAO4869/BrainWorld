[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "brainworld"
version = "0.1.0"
description = "Public BrainWorld VAE + conditional diffusion toolkit"
requires-python = ">=3.8"
dependencies = ["torch", "numpy", "matplotlib", "tqdm"]
